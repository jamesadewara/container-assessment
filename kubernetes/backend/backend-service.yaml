apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: muchtodo
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080   # Exposes on port 30080 on each node
  type: NodePort